{
  "name": "stockfolio",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ui build && pnpm --parallel --filter api --filter web --filter ui dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
