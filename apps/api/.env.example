# Neon: create a project at https://neon.tech and paste the connection string below.
# Local dev: docker run -d --name stockfolio-db -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=stockfolio -p 5432:5432 postgres:16
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stockfolio?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="30d"
PORT=4000
